import { functionalPipelinesDemo } from './functional-pipelines-demo';

describe('functionalPipelinesDemo', () => {
  it('should work', () => {
    expect(functionalPipelinesDemo()).toEqual('functional-pipelines-demo');
  });
});
